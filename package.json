{
  "name": "http2-concat-benchmark",
  "version": "1.0.0",
  "description": "Benchmark test for concatenation in HTTP/2 environment",
  "scripts": {
    "start": "http-server _build",
    "build": "node build.js"
  },
  "devDependencies": {
    "http-server": "^0.9.0"
  },
  "dependencies": {
    "lodash": "^4.12.0",
    "mkdirp": "^0.5.1",
    "randomstring": "^1.1.4",
    "shx": "^0.1.2"
  }
}
